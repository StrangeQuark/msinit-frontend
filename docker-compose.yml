services:
  msinit-frontend:
    build: .
    networks:
      - msinit-network
      - shared-network
    ports:
      - "3001:80"
    container_name: msinit-frontend
networks:
  msinit-network:
    external: true
  shared-network:
    external: true
